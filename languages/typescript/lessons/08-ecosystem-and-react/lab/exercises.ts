/**
 * LAB 08: React & Ecosystem
 * 
 * TASK:
 * 1. Define Props for a `TaskItem` component.
 * 2. Type a `useState` hook for a list of tasks.
 * 3. Type an `onChange` handler for a select dropdown.
 */

interface Task {
    id: number;
    text: string;
    isDone: boolean;
}

// TODO: Create an interface 'TaskItemProps' 
// It should take a 'task' object and an 'onToggle' function.


// TODO: Type this useState hook to hold an array of Task objects
// const [tasks, setTasks] = useState([]);


// TODO: Type this event handler for a HTMLSelectElement
// const handleSelectChange = (e) => {
//    console.log(e.target.value);
// }

export {};
