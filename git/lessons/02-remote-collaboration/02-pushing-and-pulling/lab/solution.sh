#!/bin/bash

# Lab 2 Solution

# 2. Connect Local to Remote
# git remote add origin https://github.com/your-username/git-sync-lab.git

# 3. The First Push
# git push -u origin main

# 5. The Pull
# git pull origin main

# 6. Resolving a Sync Conflict
# After a failed push:
# git pull origin main
# (Resolve conflict in file)
# git add <resolved-file>
# git commit -m "Merge remote changes and resolve conflict"
# git push
